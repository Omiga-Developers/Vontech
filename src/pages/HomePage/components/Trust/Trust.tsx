import styled from "styled-components"

const Trust = () => {
    return (
        <Container id="trust">
            trust
        </Container>
    )
}

export default Trust

const Container = styled.div `
`